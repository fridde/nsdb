{% extends 'admin/admin_base.html.twig' %}

{% block page_title 'Bekräfta buss' %}

{% block admin_page_content %}
    {# <div id="confirm-bus-orders">
    <table>
        {% for visit in visits %}
            {% set color_index = colors[visit.id] %}
            <tr data-color-index="{{ colors[visit.id] }}">
                <td>{{ visit.DateString }}</td>
                <td>{{ visit.Label }}</td>
                <td>{{ visit.Group.NumberStudents }} pax</td>
                {% set td_class, icon_class = 'toggle-label', 'fas fa-question' %}
                {% if visit.BusStatus %}
                    {% set td_class, icon_class = 'toggle-label active', 'fas fa-check' %}
                {% endif %}
                <td data-visit-id="{{ visit.id }}" class="{{ td_class }}"><i class="{{ icon_class }}"></i></td>
            </tr>
        {% endfor %}
    </table>
</div> #}
    {% if unknowns|length %}
        <h2>Okända platser</h2>
        <table>
            {% for unknown in unknowns %}
                <tr>
                    <td>{{ unknown }}</td>
                    <td>
                        <select class="location-translation" data-lookup-name="{{ unknown }}">
                            {% for key, location in locations_and_schools %}
                                <option value="{{ key }}">{{ location }}</option>
                            {% endfor %}
                        </select>
                    </td>
                </tr>
            {% endfor %}
        </table>
        <button type="button" id="save-unknown-locations">Spara platser</button>
    {% endif %}
    <ul>

    </ul>
    <ul>

    </ul>
    <ul>
        {% for visit in visits %}
            <li>{{ visit|json_encode }}</li>
        {% endfor %}
    </ul>

{% endblock %}

